apiVersion: v1
kind: Service
metadata:
  name: spring-frontend-service
spec:
  type: NodePort
  selector:
    app: spring-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
